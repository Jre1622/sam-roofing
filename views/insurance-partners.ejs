<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <meta name="description" content="<%= description %>" />

    <!-- Canonical URL -->
    <link rel="canonical" href="<%= canonical %>" />

    <!-- Social Media Meta Tags -->
    <%- include('partials/social-meta') %>

    <!-- Favicon -->
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png" />
    <link rel="manifest" href="/favicon/site.webmanifest" />

    <!-- CSS -->
    <link rel="stylesheet" href="/css/output.css" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />

    <!-- Structured Data -->
    <%- include('partials/structured-data') %>

    <!-- Add to your CSS -->
    <style>
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .animate-fade-in-up {
        opacity: 0;
        animation: fadeInUp 3s ease-out forwards;
      }
    </style>
  </head>
  <body class="flex flex-col min-h-screen bg-gray-50">
    <!-- Header -->
    <header><%- include('partials/header') %></header>

    <!-- Main Content -->
    <main class="flex-grow">
      <!-- Hero Section -->
      <section class="bg-gray-100 py-12 md:py-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="text-center max-w-3xl mx-auto">
            <h1 class="text-4xl font-bold text-gray-900 sm:text-5xl">Experienced With All Major Insurance Providers</h1>
          </div>
        </div>
      </section>

      <!-- Insurance Partners Grid Section -->
      <section class="py-12 md:py-16 bg-white">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="text-center mb-10">
            <p class="text-lg text-gray-600 max-w-3xl mx-auto">
              We're experienced in providing detailed documentation and quality repairs that meet insurance standards. <strong>If you don't see your insurance provider listed, don't worry</strong> -
              we work with all major insurance companies. Our professional team ensures your restoration project is completed to the highest standards.
            </p>
          </div>

          <div class="grid grid-cols-3 sm:grid-cols-4 lg:grid-cols-6 gap-4">
            <% insuranceCompanies.forEach((company, index) => { %>
            <div
              class="bg-white rounded-lg shadow border border-gray-200 p-3 flex flex-col items-center justify-center hover:shadow-lg hover:border-blue-200 transition-all duration-300 animate-fade-in-up"
              style="animation-delay: '<%= index * 150 %>ms'"
            >
              <div class="w-20 h-20 bg-white rounded mb-2 flex items-center justify-center">
                <img src="/images/insurance-logos/<%= company.image %>" alt="<%= company.name %> Insurance Logo" class="w-full h-full object-contain" loading="lazy" />
              </div>
              <h3 class="text-sm font-medium text-gray-900 text-center leading-tight"><%= company.name %></h3>
            </div>
            <% }); %>

            <!-- Insurance Coverage Card -->
            <div
              class="bg-gradient-to-br from-blue-50 to-gray-50 rounded-lg shadow border border-gray-200 p-3 flex flex-col items-center justify-center hover:shadow-lg hover:from-blue-100 transition-all duration-300 animate-fade-in-up cursor-pointer"
              onclick="window.location.href='/contact'"
            >
              <div class="w-20 h-20 rounded mb-2 flex items-center justify-center">
                <i class="fas fa-shield-alt text-4xl text-blue-600"></i>
              </div>
              <h3 class="text-sm font-medium text-gray-900 text-center leading-tight">Other Provider?</h3>
              <span class="text-xs text-blue-500 mt-2">No Problem! →</span>
            </div>
          </div>
        </div>
      </section>

      <!-- Contact Call to Action -->
      <section class="py-12 bg-blue-600 text-white">
        <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <h2 class="text-2xl md:text-3xl font-bold mb-4">Professional Restoration Services</h2>
          <p class="text-xl text-blue-100 mb-6">We'll restore your property while working alongside your insurance provider throughout the claims process.</p>
          <a href="/contact" class="inline-block px-6 py-3 bg-white text-blue-600 font-bold rounded-full hover:bg-gray-100 transition duration-200">Schedule Your Free Estimate</a>
        </div>
      </section>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-8 mt-10"><%- include('partials/footer') %></footer>

    <!-- JavaScript -->
    <script src="/js/main.js"></script>
    <!-- 100% privacy-first analytics -->
    <script async src="https://scripts.simpleanalyticscdn.com/latest.js"></script>
  </body>
</html>
